{
    "build": {
      "command": "composer install --optimize-autoloader --no-scripts --no-interaction"
    },
    "start": {
      "command": "php artisan serve --host=0.0.0.0 --port=$PORT"
    }
  }